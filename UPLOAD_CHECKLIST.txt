UPLOAD CHECKLIST

Upload the extracted contents of this ZIP directly to the root of your GitHub repository.

The repository root SHOULD contain:
- app/
- public/
- package.json
- tsconfig.json
- next-env.d.ts
- README.md
- .npmrc
- .gitignore

The repository root SHOULD NOT contain:
- root-level layout.tsx
- root-level page.tsx
- root-level globals.css
- package-lock.json
- any PNG/JPG/SVG files at root

Important checks in GitHub:
- package.json must be readable JSON text starting with "{"
- tsconfig.json must be readable JSON text starting with "{"
- EU logo must be in public/visuals/eu-co-funded.png

Vercel deploy settings:
- Framework preset: Next.js
- Root directory: empty
- Output directory: empty
- Install command: npm install --legacy-peer-deps --no-audit --no-fund
- Build command: npm run build

After uploading, redeploy without build cache if Vercel previously failed.
